allow surfaceflinger sysfs_msm_subsys:dir r_dir_perms;
allow surfaceflinger vendor_file:file rx_file_perms;

# allow surfaceflinger to access /dev/binderfs/hwbinder
allow surfaceflinger binderfs:chr_file { read write open ioctl map};
